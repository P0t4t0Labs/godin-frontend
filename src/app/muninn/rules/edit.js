import angular from 'angular';
import rootName from './index';

const MOD_NAME = rootName + '.edit';

angular.module(MOD_NAME, [])
  .component('muninnRulesEdit', {
    template: '<h1>Muninn Rules EDIT: TO DO</h1>'
  });


export default MOD_NAME;
