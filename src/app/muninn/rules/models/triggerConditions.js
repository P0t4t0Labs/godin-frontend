// These are formly fields
// http://docs.angular-formly.com/docs/field-configuration-object
const conditions = {
  equals: {
    display: 'equals',
    fields: [
      {}
    ]
  },
  delta: {
    display: 'delta',
    value: 'number',

  }
};

export default conditions;
